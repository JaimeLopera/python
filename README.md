# Python Database
A small database that allows the user to use some commands to edit it.
It consists in a library where all the functions are defined and the main program that call that functions to use them.
